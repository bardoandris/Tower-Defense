#include <SDL2/SDL_surface.h>
#include <map>
#include <string>

namespace game_globals {
	const int TileSize = 32;
	const int WindowHeight = 25*TileSize + 150;
	const int WindowWidth = 20*TileSize+500;
}
